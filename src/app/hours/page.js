import React from "react";
import PageLayout from "../(components)/pageLayout";

const Hours = () => {
    return (
        <>
            <PageLayout>
                <h1>Hours</h1>
            </PageLayout>
        </>
    );
};

export default Hours;
